import React from 'react';
import styled from 'reshadow';

import styles from './style.css';

const Hero = () => styled(styles)(
    <wrapper>
        <hero />
    </wrapper>
);

export default Hero;
