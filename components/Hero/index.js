import React from 'react';
import styled from 'reshadow';

const Hero = () => styled`
    hero {
        background-color: rgb(118, 48, 119);
        min-height: 300px;
        color: white;
    }
`(
    <hero>Hero</hero>
);

export default Hero;
